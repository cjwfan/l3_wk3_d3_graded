## What the app does

This React app fetches a list of random users from an API and displays their information on the page. 
The app also includes a button that lets the user show or hide the gender information.

## How to run the app
1. Install dependencies: npm install
2. In terminal, use "npm run dev" to open the url shown in your browser.

